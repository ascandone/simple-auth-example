{
  "name": "backend",
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.14.0"
  },
  "scripts": {
    "dev": "NODE_ENV=dev node --watch ./src/index.js",
    "migrate": "NODE_ENV=dev node ./scripts/migrate.mjs",
    "test": "NODE_ENV=dev node --test"
  }
}
